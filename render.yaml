services:
  - type: web
    name: merle-erik-forecast
    runtime: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
